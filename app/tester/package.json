{
  "name": "tester",
  "version": "1.3.0",
  "scripts": {
    "angular": "cross-env BASE_URL=http://localhost:5052 npx playwright test",
    "svelte": "cross-env BASE_URL=http://localhost:5050 npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "cross-env": "7.0.3"
  },
  "type": "module"
}
