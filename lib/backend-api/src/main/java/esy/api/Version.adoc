A `Version` value represents a semantic API version as a `major`.`minor` pair (see semver.org).
The `major` attribute must be increased whenever an incompatible change is introduced into the public API.
The `minor` attribute must be increased whenever new, backward-compatible functionality is released.
