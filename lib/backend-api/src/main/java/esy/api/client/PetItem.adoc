A `PetItem` value represents a simplified projection of a `Pet`, used for selection lists (dropdowns, autocomplete) instead of the full entity.
The `value` attribute holds the referenced pet's `id`.
The `text` attribute holds the pet's `species` and `name` combined, used as the display label.
