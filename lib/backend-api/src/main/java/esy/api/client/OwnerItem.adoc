An `OwnerItem` value represents a simplified projection of an `Owner`, used for selection lists (dropdowns, autocomplete) instead of the full entity.
The `value` attribute holds the referenced owner's `id`.
The `text` attribute holds the owner's `name`, used as the display label.
