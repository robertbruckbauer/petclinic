An `Invoice` entity represents a billing document issued to a client.
The `at` date is required and represents when the invoice was issued.
The `due` date is required and represents when payment is expected.
The `status` indicates the current lifecycle state of the invoice, defaulting to `D` (drafted).
The `text` is required and provides a description or notes for the invoice.
