A `VetItem` value represents a simplified projection of a `Vet`, used for selection lists (dropdowns, autocomplete) instead of the full entity.
The `value` attribute holds the referenced vet's `id`.
The `text` attribute holds the vet's `name`, used as the display label.
