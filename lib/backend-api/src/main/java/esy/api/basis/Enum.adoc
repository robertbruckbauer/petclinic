An `Enum` entity represents an extensible enumeration item.
Enums are organized by discriminator for logical grouping (e.g. "SPECIES", "SKILL").
Within each discriminator group, numeric codes and names are unique.
