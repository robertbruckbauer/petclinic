:includedir: ../..
:relrootdir: ../..
:graphqldir: {includedir}/lib/backend-data/src/main/resources/graphql
= Owner GraphQL API

GraphQL API for querying pet owners in the pet clinic application.
It provides query operations to retrieve all owners, find owners by identifier or name, and navigate the owner-pet relationship.

== Model

=== Owner

include::{includedir}/lib/backend-api/src/main/java/esy/api/client/Owner.adoc[]

NOTE: The `contact` field is declared as `contact: String` (nullable) in the GraphQL schema, but the underlying database column has `nullable=false`. The Java entity initializes `contact` to an empty string and `verify()` does not enforce a non-empty value. As a result, the API never returns `null` for `contact` — at a minimum it returns an empty string. If strict optionality semantics are needed, consider changing the schema to `contact: String!`.

The schema type `Owner` defines properties, collections and relations.

.Schema
[source,graphql,options="nowrap"]
----
include::{graphqldir}/owner.gqls[]
----

The relation `allPet` uses batch loading via `@BatchMapping` to efficiently load pets for multiple owners.

== Queries

=== Collection queries

==== `allOwner`

This query returns all pet owners in the system.

.Query Definition
[source,graphql,options="nowrap"]
----
allOwner: [Owner]
----

*Returns:*

A list of all `Owner` objects.
Returns an empty list if no owners exist.

=== Object queries

==== `ownerById`

This query returns a single owner by unique identifier.

.Query Definition
[source,graphql,options="nowrap"]
----
ownerById(id: ID!): Owner
----

*Arguments:*

`id` (ID, required)::
The unique identifier of the owner (UUID format).

*Returns:*

An `Owner` object if found, or null if no owner exists with the given identifier.

==== `ownerByName`

This query returns a single owner by unique name.

.Query Definition
[source,graphql,options="nowrap"]
----
ownerByName(name: String!): Owner
----

*Arguments:*

`name` (String, required)::
The unique name of the owner (exact match, case-sensitive).

*Returns:*

An `Owner` object if found, or null if no owner exists with the given name.
