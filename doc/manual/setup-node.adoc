:includedir: ../..
:relrootdir: ../..
= Setup node

This manual specifies the node version and related configurations for this repository.
The project uses only versions with long-term support.
Node is used for the frontend server application and for build tooling and tasks.

== Build

.Dockerfile
[source,docker]
----
include::{includedir}/app/client-angular/Dockerfile[lines=1,indent=0]
----

The angular frontend build stage depends on node.

.Dockerfile
[source,docker]
----
include::{includedir}/app/client-svelte/Dockerfile[lines=1,indent=0]
----

The svelte frontend build stage depends on node.

== Github

.build.yml
[source,gradle]
----
include::{includedir}/.github/workflows/build.yml[tag=setup-node,indent=0]
----

The GitHub actions workflow configures node for automated builds and tests.

.devcontainer.json
[source,gradle]
----
include::{includedir}/.devcontainer/devcontainer.json[tag=setup-node,indent=0]
----

The Github devcontainer adds the node feature.
